apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer
  selector:
    app: website
  ports:
    - port: 81
      nodePort: 30080
      protocol: TCP
      targetPort: 80
